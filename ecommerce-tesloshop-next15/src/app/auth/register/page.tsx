import { titleFont } from '@/config/font';

export default function AuthPage() {
    return (
        <main>
            <h1 className={titleFont.className}>Register Page</h1>
        </main>
    );
}
